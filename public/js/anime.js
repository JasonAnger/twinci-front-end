// let animeText = document.getElementById('animeText')
var options = {
    strings: ['Create', 'Buy','Sell', 'Earn', 'Collect'],
    typeSpeed: 80,
    backDelay: 900, 
    backSpeed: 60, 
    loop: true
  };
   
var typed = new Typed('#animeText', options);